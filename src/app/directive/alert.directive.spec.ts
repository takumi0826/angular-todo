import { AlertDirective } from './alert.directive'

xdescribe('AlertDirective', () => {
  it('should create an instance', () => {
    const directive = new AlertDirective()
    expect(directive).toBeTruthy()
  })
})
